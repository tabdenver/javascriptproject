let orderCount = 0;
const takeOrder = (size, topping1, topping2) => {
 console.log(`${size}Pizza with ${topping1} ${topping2}`);
 orderCount++;
console.log( orderCount)
}
takeOrder("large ", "pineapple", "Sausages")

